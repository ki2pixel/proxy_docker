# Create compute instance

> **Page context:** Oracle Cloud Infrastructure instance creation page, currently on the **Networking** step.

## Header Information

- **Account status:** Free Tier account
- **Region:** France Central (Paris)
- **Trial message:** You are in a Free Trial. When your trial is over, your account is limited to Always Free resources. Upgrade at any time. [Learn more](#)

## Instance Creation Steps

1. **Basic information** — completed
2. **Security** — completed
3. **Networking** — required
4. **Storage** — required
5. **Review** — pending

**Tasks completed status:** 2 of 4

---

## Networking

A virtual network interface card (VNIC) connects your instance to a virtual cloud network (VCN) and endpoints in and outside the VCN. Having a public IP address is required to make this instance accessible from the internet.

### Primary VNIC

- **VNIC name**

#### Primary Network

- [x] Select existing virtual cloud network
- [ ] Create new virtual cloud network
- [ ] Specify OCID

#### Subnet Selection

- [x] Select existing subnet
- [ ] Create new public subnet
- **Subnet compartment:** `camillemoinepro (root)`
- **Subnet (required)**

### Private IPv4 Address Assignment

If the VNIC must be reachable only by your cloud or on-premises resources, it must have a private IPv4 address. To assign the VNIC a private IPv4 address, select a subnet that's configured to support private IPv4 addresses. [Learn more about private IPv4 addresses](#).

- **Subnet IPv4 prefixes**
- Automatically assign private IPv4 address — Oracle assigns the next unused address in the chosen subnet to the VNIC
- Manually assign private IPv4 address — Enter an unused address in the chosen subnet for the VNIC
- Provide existing private IPv4 OCID — Enter the OCID of an existing private IPv4 address in the chosen subnet for the VNIC

### Public IPv4 Address Assignment

If the VNIC must be reachable from the internet, it must have a public IPv4 address. [Learn more about public IPv4 addresses](#).

- Automatically assign public IPv4 address
- **Info:** If you're not sure whether you need a public IP address, you can always assign one later.

> **Warning:** You must select a public subnet to assign a public IPv4 address.

### IPv6 Address Assignment

An IPv6 address can be reachable or not reachable from the internet. [Learn more about IPv6 addresses](#).

- Assign IPv6 address from subnet prefix

> **Warning:** You must select a subnet enabled to use IPv6 assign an IPv6 addresses.

## Advanced Options

### Add SSH Keys

Generate an SSH key pair to connect to the instance using a Secure Shell (SSH) connection, or upload a public key that you already have.

- [x] Generate a key pair for me
- [ ] Upload public key file (`.pub`)
- [ ] Paste public key
- [ ] No SSH keys

Download the private key so that you can connect to the instance using SSH. It will not be shown again.

- Download private key
- Download public key

---

## Footer Navigation

- Cancel
- Previous
- Next

## Privacy and Terms

- Terms of Use and Privacy
- Cookie Preferences

Copyright © 2026, Oracle and/or its affiliates. All rights reserved.